# Tools

Scripts for generating nonogram puzzles from images and building the content manifest.

Planned:
- `generate_puzzle.py` — convert image → binary grid (+ metadata)
- `build_manifest.py` — scan `content/puzzles/` → `content/manifest.json`

